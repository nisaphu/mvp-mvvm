<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.businesscard">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".GiphyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".koin.FeedActivity"></activity>
        <activity android:name=".lifecycle.LifecycleActivity" />
        <activity android:name=".fragmentation.MainFragmentActivity" />
        <activity android:name=".mvp2.view.MainMvp2Activity" />
        <activity android:name=".mvvm.view.ui.Main2Activity" />
        <activity android:name=".mvp.activity.SearchUserActivity" />
        <activity android:name=".mvp.activity.LoginActivity" />
        <activity android:name=".gameviewmodel.MainGameActivity" />
        <activity android:name=".TextStyleActivity" />
        <activity android:name=".mvvm1.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>